<div class="container">
  <div class="title">
    <h3><%= t('index.title.flatshare') %></h3>
  </div>
  <div class="row margin-top">
    <% @flatshares.each do |apartment| %>
        <%= render "shared/card", apartment: apartment %>
    <% end %>
  </div>
</div>
