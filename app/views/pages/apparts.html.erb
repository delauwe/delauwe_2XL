<div class="container">
  <div class="title">
    <h1><%= t('index.title.apartment') %></h1>
  </div>
  <div class="row margin-top">
    <% @apparts.each do |apartment| %>
        <%= render "shared/card", apartment: apartment %>
    <% end %>
  </div>
</div>
